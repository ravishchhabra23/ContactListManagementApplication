import {ModuleWithProviders} from '@angular/core';
import {Routes, RouterModule} from '@angular/router';

const commonsRoutes: Routes = [
];
export const commonsRoutingProviders: any[] = [];

export const commonsrouting: ModuleWithProviders = RouterModule.forChild(commonsRoutes);
